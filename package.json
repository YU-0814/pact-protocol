{
  "name": "pact-protocol",
  "version": "1.0.0",
  "private": true,
  "description": "PACT - Protocol for Agent Content Transfer",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "clean": "rm -rf packages/*/dist"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/pact-protocol/pact"
  },
  "dependencies": {
    "docx": "^9.6.1"
  }
}
