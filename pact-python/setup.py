from setuptools import setup, find_packages

setup(
    name='pact-protocol',
    version='1.0.0',
    description='Pure-Python SDK for the PACT protocol',
    packages=find_packages(),
    python_requires='>=3.8',
)
